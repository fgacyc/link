// import { useQuery } from "@tanstack/react-query"
// import { GraphQLClient } from "graphql-request"

// const useGQL = () => {

//     const client = new GraphQLClient(import.meta.env.VITE_GRAPHQL_URL)

//     const fetch= (key:string, query: string) => {
//         const {data} = useQuery({
//             queryKey: [key],
//             queryFn: () => fetchCg(),
//         })
//     }

//     return { fetch, mutate}
// }
